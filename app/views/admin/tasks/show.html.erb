<section>
  <div>
    <div class="two-column-row">
      <div>

        <h3><%= @task.title %></h3>

        <h4>Category <small><%= @task.category %></small></h4>
        <br>
        <p><%= @task.description %></p>

        <%=
          link_to "Edit",
            edit_admin_task_path(@task),
            class: "btn btn-primary"
        %>
      </div>

    </div>
</div>
</section>

