require "rails_helper"

describe InvitationToken do

  #describe "#before_create" do
    #it "should create a unique token" do
      #invitation = create(:invitation_token)

      #expect(invitation.token.present?).to eql(true)
    #end
  #end
end
