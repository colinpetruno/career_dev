<section>
  <div>

    <div class="two-column-row">
      <div>
        <h1>Tasks</h1>
      </div>
      <div class="text-right">
        <%= link_to "New Task", new_company_task_path(current_company), class: "button" %>
      </div>
    </div>

    <% if @tasks.present? %>
      <%= render partial: "tasks" %>
    <% else %>
      No tasks exist yet, TODO: Make some better copy for this
    <% end %>
  </div>
</section>
